from setuptools import setup

setup(
    name='tigercopter',
    version='0.1dev',
    packages=['tigercopter',],
    license='MIT',
    long_description=open('README.md').read(),
    install_requires=[
    ]
)
